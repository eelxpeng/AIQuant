[package]
name = "types"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

# Intentionally empty, and asserted by TEST-023 in tests/structure.rs.
# `types` is the bottom of the dependency order (Constitution I).
[dependencies]

[dev-dependencies]
proptest = "1"
trybuild = "1"
