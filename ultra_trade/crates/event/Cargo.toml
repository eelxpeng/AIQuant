[package]
name = "event"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
types = { path = "../types" }

[dev-dependencies]
proptest = "1"
