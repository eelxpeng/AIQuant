[package]
name = "simkit"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
types = { path = "../types" }
event = { path = "../event" }
engine = { path = "../engine" }
oms = { path = "../oms" }
strategy = { path = "../strategy" }
